/**
 */
define(['can/util/string', 'can/model'], function( can ){
    'use strict';

    return can.Model({

        defaults: {
            'bookingId': '',
            'iframeURL': '',
            'payMethod': ''
        }
    }, {

    });

});