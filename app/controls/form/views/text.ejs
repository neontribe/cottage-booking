<input
    name="<%= attrName %>"
    type="text"
    data-type="<%= type %>"
    data-label="<%= label %>"
    class="<% classes %>"
    value="<%= model.attr( attrName ) ? model.attr( attrName ) : '' %>"
    <%= (el) -> el.tooltip( tooltipOptions ) %>
/>
