<%# If the value at the display is === true or undefined then display %>
<% if( display.attr( attrName + '()' ) !== false ) { %>
    <div class="form-item" <%= appendTemplate( view, this ) %>>
        <% if( label ) { %>
            <label for="<%= id %>"><%= label %></label>
        <% } %>
        <%# Template gets appended %>
    </div>
<% } %>